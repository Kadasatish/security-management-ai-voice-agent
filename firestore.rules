rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Keep the database closed until application roles and authorization rules are defined.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
